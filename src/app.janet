(import joy :prefix "")
(import ./layout :as layout)
(import ./routes :as routes)

(def app (-> (app routes/app)
             (db (env :db-name))
             (layout layout/app)
             (session)
             (body-parser)
             (extra-methods)
             (query-string)
             (server-error)
             (logger)
             (static-files)))
